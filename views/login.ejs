<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/icon-font.css">
    <link rel="stylesheet" href="/css/style.css">
    <title>Login</title>

    <style>
        body {
            background-image: url('/images/chilling.jpg');
            background-attachment: fixed;
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
        }

        #register {
            width: 12rem;
            display: block;
            margin: 2rem 0 0 2rem;
        }

        #container {   
            display: block;
            padding-bottom: 2rem ;
            text-align: center;
            overflow: hidden;
        }

        form {
            justify-content: center;
            align-items: center;
            width: 40rem;
            display: inline-block;
            margin: auto;
            text-align: center;
        }

        #warning {
            display: block;
            margin: 3rem auto auto auto;
            padding: 1rem;
            background-color: rgb(254, 135, 135);
            border: 1px solid rgb(254, 135, 135);
            border-radius: 2px;
            color: rgb(0, 0, 0);
            width: 40rem;
        }

        input {
            margin: auto
        }
    </style>
</head>

<body>
    <div id="container">
        <a href="/register" class="btn btn-text" id="register"> Register &rarr; </a>
        <form action="/auth/login" method="POST" class="form">
            <div class=" u-margin-bottom-medium"> <!--utility class with goal to centerr the text inside it -->
                <h2 class="heading-secondary">
                    Login Here
                </h2>
            </div>
            <div class="form__group">
                <input type="email" class="form__input" placeholder="Email Address" required id="email" name="email">
            </div>
            <div class="form__group">
                <input type="password" class="form__input" placeholder="Password" required id="password"
                    name="enteredPassword">
            </div>
            <div class="form__group">
                <button class="btn btn--green">
                    LOGIN
                </button>
            </div>
        </form>

        <% if (typeof message !=='undefined' && message.length !=0) { %>
            <div id="warning">
                <h2><%= message %></h2>
            </div>
        <% } %>
    </div>
    
</body>

</html>